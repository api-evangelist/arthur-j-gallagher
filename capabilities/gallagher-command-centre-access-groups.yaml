naftiko: 1.0.0-alpha2
info:
  label: Gallagher Command Centre REST API — Access Groups
  description: 'Gallagher Command Centre REST API — Access Groups. 1 operations. Lead operation: List Access Groups. Self-contained
    Naftiko capability covering one Arthur J Gallagher business surface.'
  tags:
  - Arthur J Gallagher
  - Access Groups
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ARTHUR_J_GALLAGHER_API_KEY: ARTHUR_J_GALLAGHER_API_KEY
capability:
  consumes:
  - type: http
    namespace: gallagher-command-centre-access-groups
    baseUri: https://localhost:8904/api
    description: Gallagher Command Centre REST API — Access Groups business capability. Self-contained, no shared references.
    resources:
    - name: access_groups
      path: /access_groups
      operations:
      - name: listaccessgroups
        method: GET
        description: List Access Groups
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: gallagher-command-centre-access-groups-rest
    port: 8080
    description: REST adapter for Gallagher Command Centre REST API — Access Groups. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/access-groups
      name: access-groups
      description: REST surface for access_groups.
      operations:
      - method: GET
        name: listaccessgroups
        description: List Access Groups
        call: gallagher-command-centre-access-groups.listaccessgroups
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: gallagher-command-centre-access-groups-mcp
    port: 9090
    transport: http
    description: MCP adapter for Gallagher Command Centre REST API — Access Groups. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: list-access-groups
      description: List Access Groups
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: gallagher-command-centre-access-groups.listaccessgroups
      outputParameters:
      - type: object
        mapping: $.
